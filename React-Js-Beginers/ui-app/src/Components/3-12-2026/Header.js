function Header(params) {
  return (
    <header
      style={{
        background: "#282c34",
        padding: "20px",
        color: "white",
        textAlign: "center",
      }}
    >
      <h1>My Simple Header</h1>
    </header>
  );
}

export default Header;
